import MuiUploadIcon from "@mui/icons-material/Upload";

export default function UploadIcon({ className }: { className?: string }) {
  return <MuiUploadIcon className={className} fontSize={"small"} />;
}
